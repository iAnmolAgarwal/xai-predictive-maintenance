# Build outputs and reports
dist
dist-mock
coverage
playwright-report
test-results
node_modules

# Lockfile: pnpm owns its formatting
pnpm-lock.yaml

# Generated by `make contracts`, never hand-edited (R15)
src/contracts/api.ts
src/contracts/ws.ts

# Vendored by `msw init`
mock-public/mockServiceWorker.js

# Transcribed verbatim from docs/plan/frontend.md §6.2 and diffed against it;
# re-wrapping the paired custom properties would break that comparison.
src/styles/tokens.css
